# PostPress AI — repo hygiene (plugin folder scoped)

# Server-created logs (never commit)
php_errorlog

# Temporary folders used for confirmation tests
inc/admin/_backup_OFF/

# Local one-off safety backups (never commit)
inc/class-ppa-controller.php.bak.*
inc/*.bak.*
*.bak.*

# Editor junk
*.swp
*.swo
*.tmp
.DS_Store
